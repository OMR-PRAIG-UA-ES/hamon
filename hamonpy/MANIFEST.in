include LICENSE
include README.md
# The test suite is data-driven over fixtures that live in the repository, not in the
# package, so shipping it would hand users a suite that cannot pass. It stays in the repo.
prune tests
